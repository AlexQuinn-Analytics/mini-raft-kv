package com.miniraftkv.log;

public class LogEntry {
    private long term;
    private int index;
    private String command;

    public LogEntry(long term, int index, String command){
        this.term = term;
        this.index = index;
        this.command = command; 
    }

    public long getTerm(){
        return term;
    }

    public int getIndex(){
        return index;
    }

    public String getCommand(){
        return command;
    }

    @Override
    public String toString(){
        return "LogEntry{term=" + term + ",index=" + index + ",command='" + command + "'}";
    }
    
}
